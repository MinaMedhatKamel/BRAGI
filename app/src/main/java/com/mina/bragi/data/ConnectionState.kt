package com.mina.bragi.data

enum class ConnectionOptions(name: String) {
    CONNECTION_ERROR("connection error"),
    CONNECTING("connecting"),
    CONNECTION_ESTABLISHED("connection established")
}